{
  "name": "twsql",
  "version": "0.1.2",
  "description": "Twitter client that uses sql commands rather than a stream",
  "main": "twsql.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "deadgar",
  "license": "ISC",
  "dependencies": {
    "console.table": "^0.9.1",
    "node-sqlparser": "^1.0.2",
    "twit": "^2.2.9",
    "word-wrap": "^1.2.3"
  }
}
